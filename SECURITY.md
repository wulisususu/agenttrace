# Security Policy

## 项目安全边界

AgentTrace 面向本地开发仓库，因此必须假设日志和环境中可能出现敏感信息。

默认策略：

- 本地处理；
- 默认只读；
- 不上传日志；
- 不采集凭据；
- 不自动执行破坏性修复。

## 敏感信息

报告和测试 fixture 不应包含真实：

- API Key；
- Access Token；
- Cookie；
- SSH 私钥；
- 数据库密码；
- 私有服务凭据；
- 带认证信息的远端 URL。

## 日志处理

未来实现 reporter 时应在输出边界加入脱敏能力。

即使原始日志包含秘密信息，默认结构化报告也不应无条件完整回显原始日志。

## 命令执行

如果后续 collector 需要调用系统命令：

- 参数必须结构化；
- 避免拼接不可信 shell 字符串；
- Windows 与 Unix 行为分别测试；
- 不把仓库内容直接当作 shell 命令执行。

## 自动修复

MVP 不实现自动修复。

未来任何会修改用户仓库的能力必须：

1. 明确展示动作；
2. 默认关闭；
3. 要求用户主动确认；
4. 提供可审计结果；
5. 避免不可逆操作。

## 报告安全问题

如果发现可能泄露凭据、执行任意命令或破坏仓库的问题，请不要在公开 Issue 中粘贴秘密信息。优先使用 GitHub 提供的私密安全报告渠道（仓库启用后）。
