name = "wulisususu/agenttrace"
version = "0.1.0"
license = "Apache-2.0"
description = "Reusable MoonBit diagnostics core with evidence models, deterministic rules, adapters, and reporters"

import {
  "moonbitlang/async@0.20.2",
}

preferred_target = "native"
